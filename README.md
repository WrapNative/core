# WrapNative Core

> Turn any web project into a native-like PWA with zero build config.

**WrapNative** is a lightweight JavaScript engine designed to "wrap" standard web applications with native mobile capabilities. It provides a robust App Shell structure, reactive state management, and a seamless bridge to hardware features like Camera and Haptics—all without complex frameworks or bundlers.

## Key Features
- ✨**Zero Config:** Just import the script. No Webpack/Vite required.
- ⚡**Reactive Store:** Built-in `state.reactive` for instant UI updates.
- 📱**Native Transitions:** Smooth page navigation using the View Transitions API.
- 🔌**Hardware Bridge:** Easy access to device capabilities (Camera, Haptics).
- 🎨**Clean Architecture:** Separate your core logic from your UI.
